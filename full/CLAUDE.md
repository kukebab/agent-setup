<!-- This file mirrors AGENTS.md for Claude Code compatibility. Keep both in sync. -->

# Acme Notes — AI Agent Schema (Advanced Mode)

> This file is a stub. The full agent-os schema — memory protocol, agent dispatch, source-of-truth map, rules, skills, hooks, and domain-agent state — lives in `agent/`.

**Start here → `agent/AGENTS.md`.** Read it at the start of every session; it tells you what to read next and where things live.

Installed schema version: see `agent/VERSION.md`. To check for and apply updates from the source repo, see `UPDATE_PROMPT.md`.
