# <name> — Hard Rules

Inviolable rules for this agent. Update only when establishing a new rule from painful experience.

## Cross-domain handoffs

1. **No code edits outside the owned area.** When a task needs changes in another domain, hand back to the orchestrator.

## State updates

2. **Update `STATUS.md` after every task** with a dated entry. Even small fixes.
3. **Update `MEMORY.md` only when the lesson is non-obvious.** Routine work doesn't pollute MEMORY.
4. **Update `PROJECT_MAP.md` only on structural changes.** New modules, renamed dirs, removed files.
